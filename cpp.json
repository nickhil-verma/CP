{
	"Code Forces ": {
  "prefix": "cppboilerplate",
  "body": [
    "#include <bits/stdc++.h>",
    "using namespace std;",
    "",
    "// Type aliases for convenience",
    "using ll = long long;",
    "using vi = vector<int>;",
    "using vll = vector<long long>;",
    "using pii = pair<int, int>;",
    "using pll = pair<long long, long long>;",
    "",
    "// Constants",
    "const int MOD = 1e9 + 7;",
    "const int INF = INT_MAX;",
    "const long long LINF = LLONG_MAX;",
    "",
    "// Fast I/O",
    "void fast_io() {",
    "    ios_base::sync_with_stdio(false);",
    "    cin.tie(nullptr);",
    "    cout.tie(nullptr);",
    "}",
    "",
    "// Debugging tools (can be removed or commented out in the actual competition)",
    "#define DEBUG",
    "#ifdef DEBUG",
    "#define debug(x) cerr << #x << \" = \" << (x) << endl",
    "#else",
    "#define debug(x)",
    "#endif",
    "",
    "// Function templates for modular arithmetic",
    "int add(int a, int b, int mod = MOD) {",
    "    return (a + b) % mod;",
    "}",
    "",
    "int sub(int a, int b, int mod = MOD) {",
    "    return (a - b + mod) % mod;",
    "}",
    "",
    "int mul(int a, int b, int mod = MOD) {",
    "    return (int)((1LL * a * b) % mod);",
    "}",
    "",
    "int binpow(int a, int b, int mod = MOD) {",
    "    int res = 1;",
    "    while (b > 0) {",
    "        if (b & 1) res = mul(res, a, mod);",
    "        a = mul(a, a, mod);",
    "        b >>= 1;",
    "    }",
    "    return res;",
    "}",
    "//Solve Function",
    "void solve(){",
    "  ",
    "",
    "",
    "}",
    "// Main function",
    "int main() {",
    "    fast_io();",
    "",
    "    // Input section",
    "    int t;",
    "    cin >> t;",
    "    while (t--) {",
    "        solve();",
    "",
    "    }",
    "    return 0;",
    "}",
    ""
  ],
  "description": "Code Forces "
}
}